---
const { json } = Astro.props;
---

<div class="mt-12">
  <div class="flex items-center gap-x-2">
    <img src="/user-code.svg" alt="" />
    <h2 class="text-2xl">Sobre mí</h2>
  </div>
  <p class="mt-6">
    {json.description}
  </p>
  <h3 class="text-lg mt-6">Estudios</h3>
  <div class="lg:flex lg:flex-wrap">
    {
      json.school.map((el) => (
        <p class="flex gap-x-2 mt-6 lg:w-full">
          <img src={el.icon} alt="icono" />
          <span>{el.title}</span>
        </p>
      ))
    }
  </div>
</div>
